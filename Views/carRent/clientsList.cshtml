@model IEnumerable<lab5.Models.Client>

@{
    ViewData["Title"] = "Список клиентов";
}

<h2>Список клиентов</h2>

<table class="table">
    <thead>
        <tr>
            <th>Имя</th>
            <th>Возраст</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var client in Model)
        {
                <tr>
                    <td>@client.name</td>
                    <td>@client.age</td>
                </tr>
        }
    </tbody>
</table>